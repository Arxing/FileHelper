apply plugin: 'java-library'

dependencies {
    implementation fileTree(include: ['*.jar'], dir: 'libs')
    implementation project(':fileHelper')
}

sourceCompatibility = "1.8"
targetCompatibility = "1.8"
